#ifndef GAME_ROTATION_HPP
#define GAME_ROTATION_HPP

#include <cstdint>

enum class Rotation : uint8_t {
        Up = 0,
        Right = 1,
        Down = 2,
        Left = 3
};

#endif //GAME_ROTATION_HPP
